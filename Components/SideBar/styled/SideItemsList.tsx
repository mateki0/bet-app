import styled from 'styled-components';

const SideItemsList = styled.ul`
  margin: 0;
  padding: 0;
  list-style-type: none;
  display: flex;
  flex-direction: column;
`;

export default SideItemsList;
